import { render, screen } from '@testing-library/react';
import { {{pascalCase }}} from './{{pascalCase}}';

describe('{{pascalCase}}', () => {
    test('Sucsess ', () => {
        
        expect().toBe();
    });

    test('Error', () => {
        
        expect().toBe();
    });
});
